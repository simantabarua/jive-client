import axios from "axios";

const Home = () => {
  const genToken = () => {
    
  };
  return (
    <div>
      <button onClick={genToken} className="btn btn-primary">
        gen
      </button>
    </div>
  );
};

export default Home;
